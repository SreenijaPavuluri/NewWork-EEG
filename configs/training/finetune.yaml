# STELLA finetuning configuration
name: finetune

seed: 42
n_epochs: 50
batch_size: 32
lr: 1.0e-4
lr_few_shot: 5.0e-5
weight_decay: 1.0e-4
head_dropout: 0.3
patience: 15

# Linear probe config
linear_probe:
  n_epochs: 30
  lr: 1.0e-2
  batch_size: 64
  patience: 10
